# X-INSIGHT Progress Tracker

Session: S01 (status: complete)
Outcome and FR/NFR covered: Reproducible dev loop (NFR-01, NFR-05). Failing health test → minimal FastAPI route; minimal React/Vite X-INSIGHT shell; locked installs; Make targets; disposable PostgreSQL dev/test DBs.
Files/migrations/content versions changed: backend/src/x_insight/app.py (health route); web/{package.json,package-lock.json,index.html,vite.config.ts,tsconfig.json,src/main.tsx,src/app/App.tsx,Dockerfile}; backend/Dockerfile; compose.yaml; Makefile; .env.example; .github/workflows/ci.yml. No migrations, no clinical content.
Approved seams exercised: T1/T9 smoke only (health endpoint via TestClient + real HTTP; shell via served page).
Red evidence: backend/tests/http/test_health.py → 404 on GET /api/v1/health before route existed.
Green evidence: make verify → ruff/mypy/tsc/vite build pass, 1 passed. Real-HTTP smoke: uvicorn → {"status":"ok"}; vite preview → page contains X-INSIGHT. Runtimes: Python 3.11.16, Node 22.23.2, PostgreSQL 14.24, React 19.1.1, Vite 7.1.7, fastapi 0.141.1, pgmpy 1.1.2, mcp (import ok).
Review/limitations/unrun checks: test-web/test-e2e/test-recovery/test-load correctly report "no suite yet" (exit 1). compose services not runtime-tested (no docker daemon). Ports: db 5432, api 8000, web 5173. Stop: Ctrl-C (make dev) / drop xinsight_dev|xinsight_test DBs. No credentials in tracked files (only local-dev placeholder password).
Content approvals: n/a (no content).
Remaining work and next eligible session: S02 (real database wiring/migrations per tasks.md).

Session: S02 (status: complete)
Outcome and FR/NFR covered: Real persistence + request contracts (NFR-04–05, cross-cutting FR-42). Liveness stays DB-free; readiness reports ready/unavailable/incompatible-schema; request correlation + standard error envelope without tracebacks; 1 MiB body limit + structured 422 field errors; one shared transaction context with transaction-scoped audit insert; canonical JSON/hash + UTC helpers documented at future uses.
Files/migrations/content versions changed: backend/src/x_insight/{__init__.py,db.py,contracts.py,app.py,operations/__init__.py,operations/audit.py}; backend/{alembic.ini,migrations/env.py,migrations/versions/0001_init.py} (revision 0001: audit_events); backend/tests/{conftest.py,http/test_contracts.py}; Makefile (migrate runs alembic upgrade head); backend/{pyproject.toml,uv.lock} (sqlalchemy==2.0.54, alembic==1.20.0). No clinical content.
Approved seams exercised: T1 (HTTP via TestClient + real uvicorn smoke).
Red evidence: backend/tests/http/test_contracts.py → 404 on GET /api/v1/ready before route existed (3 failed); later ImportError for register_exception_handlers before handler-registration refactor.
Green evidence: 9 passed (test_contracts 8 + test_health 1); make check (ruff/mypy/tsc/vite build) pass; make migrate on fresh scratch DB → 0001 then readiness ok; real-HTTP smoke: fresh DB /ready 200 {"status":"ready"}; bad-URL server /ready 503 UNAVAILABLE envelope with matching x-request-id, no DSN/traceback. Disposable script verified audit rollback/commit + canonical/hash/UTC/If-Match/Idempotency-Key parsing (rows cleaned up, not committed as tests). Runtimes: Python 3.11.16, PostgreSQL 14.24, sqlalchemy 2.0.54, alembic 1.20.0.
Review/limitations/unrun checks: Logical DB roles only (app/migration/readonly env vars fall back to one local role; no CREATE ROLE SQL until privilege-sensitive domain tables land in S03/S06). 422 handler proven via test-local route reusing production registration (no test-only production endpoint). Audit/canonical helpers verified disposably; first HTTP-level audit behavior lands with S03 mutations. Size limit enforces Content-Length only (chunked edge noted). compose not runtime-tested. test-web/test-e2e/test-recovery/test-load still correctly report no suite yet.
Content approvals: n/a (synthetic probe strings only, never released).
Remaining work and next eligible session: S03 (login/sessions/own credentials, first audit-covered mutations) and S15 (DDI parse) are eligible; S03 uses db.transaction + record_audit + If-Match/Idempotency-Key helpers from this session.

Session: S03 (status: complete)
Outcome and FR/NFR covered: Login, sessions, own credentials (FR-01–02, FR-04, NFR-02). Singleton admin/admin seed (idempotent, preserves changed password, DB partial unique index rejects second admin); stdlib PBKDF2-SHA256 hashing (600k iterations, exact passwords, no trimming/complexity); opaque cookie sessions (HttpOnly/SameSite-Lax, Secure under HTTPS, SHA-256 stored) with /me; generic 401 for wrong credentials/role mismatch/inactive; CSRF double-submit for logout/password/preferences; 5-fail/60s login throttling with 429; logout/password-change revocation via credential_revision + revoked_at (current kept, others revoked); no idle/absolute timeout (controlled-clock advance still valid); admin username immutable (preferences extra=forbid) and no self-registration route; empty passwords 422; audit auth.login_success/failure/logout/password_change with UUID refs only, no passwords/hashes.
Files/migrations/content versions changed: backend/src/x_insight/identity/{__init__.py,hashing.py,clock.py,throttle.py,store.py,routes.py}; backend/src/x_insight/{app.py (mount identity router /api/v1),db.py (EXPECTED_SCHEMA_REVISION 0002)}; backend/migrations/versions/0002_identity.py (revision 0002: users/sessions, ck role/theme, one_admin_only partial unique, token_hash/user indexes); backend/tests/http/test_identity.py (17 tests). No clinical content.
Approved seams exercised: T1 (Authenticated HTTP, real PostgreSQL via TestClient).
Red evidence: tests/http/test_identity.py slice1 → 404 POST /auth/login before routes (1 failed); slice2 → 404 PATCH /me/preferences (expected 403) and 401 instead of 429 for throttling (2 failed, 7 passed); slice3 → 404 POST /auth/logout and POST /me/password (3 failed, 10 passed); slice4 → empty audit_events, missing auth.login_failure (1 failed, 16 passed).
Green evidence: make test-backend TEST=tests/http/test_identity.py → 17 passed; backend pytest tests → 26 passed (8 contracts + 1 health + 17 identity); make check → ruff/mypy/tsc/vite build pass. Cookie/HTTPS probe (disposable, DB cleaned): http login 200 HttpOnly SameSite=lax without Secure, /me 200; https base_url login 200 with Secure; wrong login 401 generic UNAUTHENTICATED, no traceback/pbkdf2 in body. Runtimes: Python 3.11.16, PostgreSQL 14.24, fastapi 0.141.1, sqlalchemy 2.0.54, alembic 1.20.0.
Review/limitations/unrun checks: Throttling is process-local (ponytail ceiling: move to shared DB counter if multi-worker contention measured). HTTPS enforcement outside localhost is edge/deploy concern (S57); app sets Secure under HTTPS/X-Forwarded-Proto. Audit verified via direct audit_events read until S52 admin audit HTTP lands (interim side-channel, documented). No physician fixtures yet (S04 creates physicians; inactive test uses admin deactivation). test-web/test-e2e/test-recovery/test-load still no suite (expected). compose not runtime-tested.
Content approvals: n/a (no clinical content; synthetic passwords only, never released).
Remaining work and next eligible session: S04 (physician administration, revocation, idempotency) and S05 (role navigation/themes/browser) are eligible; S15 remains eligible in parallel. Handoff: session revocation = revoked_at + credential_revision mismatch (logout revokes current; password change bumps revision, revokes others, updates current); authenticated test-fixture interface = TestClient(app) + POST /api/v1/auth/login {username,password,role} → xinsight_session (HttpOnly) + xinsight_csrf cookies; mutating own-credential routes require X-CSRF-Token header matching xinsight_csrf; per-test isolation = TRUNCATE sessions,users + throttle.reset_all() (audit via conftest truncate); controlled clock = x_insight.identity.clock.set_now_fn/reset_now_fn (sessions ignore it by design).

Session: S04 (status: in_progress)
Outcome and FR/NFR covered: Physician account administration (FR-03–04, FR-42), using approved T1 HTTP and real PostgreSQL. Tests written/run by dev-test, sequential red–green slices.
Files/migrations/content versions changed: backend/src/x_insight/identity/accounts.py; identity/routes.py; app.py; db.py; migrations/versions/0003_physicians.py; tests/http/test_physicians.py. No clinical content.
Approved seams exercised: T1. S03 dependency reverified: 17 passed.
Red evidence: make test-backend TEST=tests/http/test_physicians.py: slice 1 POST /physicians 404 instead of 201; slice 2 PATCH password 422 instead of 200; slice 3 GET deactivation-review 404 instead of 200.
Green evidence: slices 1–2: 2 passed. Disposable migration 0003→0002→0003 passed; repairs S03 theme default containing literal quote characters (ck_users_theme violation exposed by physician creation).
Review/limitations/unrun checks: Final checks pending. Draft/job integration remains incomplete until S51; current reviewed draft set is empty because Cases storage does not exist.
Content approvals: n/a.
Remaining work and next eligible session: Verify slice 3; implement slice 4 idempotency/conflicts/safe audit; review, checks, final handoff. S05 and S15 remain independently eligible.

Session: S05 (status: complete)
Outcome and FR/NFR covered: Role navigation and both themes (FR-01, FR-03, NFR-03) via approved T1/T9. Login → role dashboard; /register "Contact administrator"; every physician login gates on the exact research warning with Continue; admin Physicians page (create + table ?limit=100), physician guard ("Access denied.", no table), own Change password on dashboards, Sign out; deliberate light/dark tokens persisted via PATCH /me/preferences (data-theme + distinct body background); keyboard/focus, associated labels, role=alert generic errors, reduced-motion rule, real loading/empty/error states. Tests written/run by dev-test, one slice per red–green cycle.
Files/migrations/content versions changed: web/src/app/App.tsx (login/dashboards/notice-gate/nav/guards/password/physicians/theme-toggle); web/src/app/api.ts (new: session/login/logout/password/physicians/theme client); web/src/shared/theme.css (new: light/dark tokens with measured ratios); web/package.json + package-lock.json (@playwright/test 1.63.0 devDep); playwright.config.ts (new: backend :8000 on xinsight_test + vite :5173, chromium+firefox); e2e/identity.spec.ts (7 tests), e2e/themes.spec.ts (3), e2e/accessibility.spec.ts (8); Makefile test-e2e resolves runner via NODE_PATH+web binary. No backend changes, no migrations, no clinical content.
Approved seams exercised: T1 (existing S03/S04 routes reused, no new endpoints) / T9 (browser journeys vs real app + real PostgreSQL).
Red evidence: slice 1 — e2e specs missing, App.tsx bare h1 (3 timeouts); slice 2 — no physicians link/guard/password/sign-out (4 timeouts); slice 3 — no theme toggle, light-only tokens (2 timeouts); slice 4 — already green, no red (prior slices covered it).
Green evidence: make test-e2e → 36 passed (18 specs × chromium+firefox, 7.9s); make test-e2e TEST=accessibility → 16 passed; TEST=identity → 14 passed; TEST=themes → 6 passed; backend regression 17 + 5 passed; web tsc + vite build pass. Runtimes: Python 3.11.16, PostgreSQL 14.24 (xinsight_test @0003), Node 22.23.2, Playwright 1.63.0 (chromium-1243 + firefox). Contrast: light action #06776B/white 5.44, muted #6B7280 4.83, urgent #DC2626 4.83, warning #92400E 7.09, normal #047857 5.48, info #0369A1 5.93, follow-up #7C3AED 5.70; dark canvas #0F172A ink #F9FAFB 17.08, muted #A8B0BA 8.15, action #2DD4BF/dark-text 9.59, all dark semantics ≥6.4.
Review/limitations/unrun checks: make check red on pre-existing S04 mypy RowMapping arg-type errors in backend/src/x_insight/identity/accounts.py:244,291,344,400 (untouched by S05; left for S04 completion). Spec race fixed test-side (wait "Password changed." before re-login). Parallel backend suites in one turn deadlock on TRUNCATE (run sequentially). .claude/ + AGENTS.md graphify block are pre-existing environment artifacts, untouched. No firefox install needed (pre-seeded). compose not runtime-tested.
Content approvals: n/a (no clinical content; synthetic e2e credentials only, never released).
Remaining work and next eligible session: S06 (patients/drafts browser + HTTP) unblocked on T1/T9 shell; S04 must still close (slices 3–4, mypy clean); S15 eligible in parallel.

Session: S06 (status: complete)
Outcome and FR/NFR covered: Register and find a patient (FR-10, FR-23) via approved T1/T9. Physician POST /patients atomically creates patient + registration draft (server timestamp, revision 1); admin POST 403, anon 401; ID 0012345678 round-trips as TEXT. Strict validation (NFC Unicode-letter names, StrictInt age 18-99, ^[0-9]{10}$ ASCII ID, required sex/status, valid decomposed Jose+acute/Muller case). Global UNIQUE(patient_id_text) gives exactly-one under 10-thread concurrent HTTP + archived-ID conflict; same Idempotency-Key+body replays original IDs, changed body 409. Directory GET /patients shared across physicians/admin with q name/ID ILIKE, clinical_status filter, archived=false default (?archived=true only-archived), bounded cursor pagination (default 25/max 100, ORDER BY id, next_cursor). Browser demographics form gates Register/Next disabled until valid; directory search + shared-visibility journeys pass. Tests written/run by dev-test subagent, one slice per red-green cycle.
Files/migrations/content versions changed: backend/src/x_insight/cases/__init__.py (new), cases/patients.py (POST /patients + GET /patients, strict PatientCreate, advisory-lock idempotency, 409 duplicate mapping, audit patient.create); backend/migrations/versions/0004_patients.py (patients + encounters, TEXT unique ID, CHECKs, ix_encounters_patient_id); db.py EXPECTED_SCHEMA_REVISION 0003->0004; app.py mounts patients router; backend/tests/http/test_patients.py (4 tests); web/src/app/api.ts (Patient/listPatients/createPatient); web/src/app/App.tsx (PatientsSection directory + physician PatientForm); e2e/registration.spec.ts (3 tests). No clinical content.
Approved seams exercised: T1 (HTTP vs real PostgreSQL) / T9 (browser vs real app + real PostgreSQL).
Red evidence: slice1 test_patients.py -> UndefinedTable patients (no tables/route); slice2 new validation test -> age 17 hit DB CHECK -> 500 not 422; slice3 sensitivity probe (mutated constraint-name match) -> UniqueViolation 500 not 409, reverted; slice4 GET /patients -> 405, e2e submit button timeout (no form).
Green evidence: backend pytest tests/http/test_patients.py -> 4 passed; regression test_identity.py 17 passed + test_physicians.py 5 passed (26 total with patients); ruff check + format clean; mypy cases/patients.py clean (4 pre-existing accounts.py RowMapping errors untouched); alembic upgrade head (0003->0004) + downgrade -1 + upgrade head ok; web npm run check clean; playwright registration --project=chromium -> 3 passed; full playwright suite per slice4 subagent -> 42 passed. Runtimes: Python 3.11.16, PostgreSQL 14.24, Node 22.23.2, pytest 9.1.1, chromium+firefox.
Review/limitations/unrun checks: Archived default hides archived (?archived=true returns only archived; no update/archive routes — archived setup is direct-SQL in test only). Ordering ORDER BY id (stable cursor) not (created_at,id). q="" matches all; client omits empty param. No delete/merge; ID stored TEXT only. S04 still in_progress with pre-existing mypy errors; compose not runtime-tested.
Content approvals: n/a (synthetic names/IDs only, never released).
Remaining work and next eligible session: S07 (author-owned drafts/autosave/discard); S15 eligible in parallel.

Session: S07 (status: complete)
Outcome and FR/NFR covered: Persist, resume, and discard author-owned drafts (FR-16, FR-22, NFR-04) via approved T1/T9. Author PATCH/GET /encounters/{id} with If-Match/ETag revisions persists draft_data across restart (new TestClient with same cookies); other physician/admin GET allowed but PATCH/discard 403; anon 401; missing/stale If-Match 412 STALE_REVISION; terminal (discarded/signed) + archived-patient PATCH 409; non-object draft_data 422. Draft editor autosaves with ~1s debounce + beforeunload/in-app Close guards, shows Saving…/Saved (rev N)/Save failed/Stale revision, retains edits on 412/failure, Reload draft refetches server preview while preserving local edits, read-only when author mismatch. Failed/aborted PATCH never shows Saved. Registration form shows no Saved before submit. Author discard POST /encounters/{id}/discard with If-Match + {"confirm": true} bumps revision to discarded, retains tombstone (GET readable, draft_data preserved, PATCH/second-discard 409) + audit encounter.discard, patient retained in directory; confirm false/missing 422, stale 412, non-author 403. Jobs cancellation is a no-op (no jobs tables; S51 integrates). Tests written/run by dev-test subagent, one slice per red-green cycle.
Files/migrations/content versions changed: backend/src/x_insight/cases/encounters.py (new: GET /patients/{id}/encounters listing, GET/PATCH /encounters/{id}, POST /encounters/{id}/discard; author-only writes, 401/403/404/409/412/422, audit encounter.update/discard); backend/migrations/versions/0005_drafts.py (new: encounters.draft_data JSONB default '{}', ck_encounters_state draft/review_ready/signed/discarded); db.py EXPECTED_SCHEMA_REVISION 0004->0005; app.py mounts encounters router (import-order fix); backend/tests/http/test_drafts.py (new: 11 tests); web/src/app/api.ts (Encounter/listEncounters/getEncounter/patchEncounter/discardEncounter); web/src/app/App.tsx (PatientsSection Open draft <patient_id> + localStorage restart recovery, DraftEditor single autosave path with revision ownership, Close/draft-switch confirm guards, Discard draft with tombstone); e2e/autosave.spec.ts (new: 2 tests), e2e/draft-safety.spec.ts (new: 2 tests), e2e/discard.spec.ts (new: 1 test). No clinical content (synthetic note values only).
Approved seams exercised: T1 (HTTP vs real PostgreSQL) / T9 (browser vs real app + real PostgreSQL).
Red evidence: slice1 PATCH /encounters/{id} 404 vs 200 (5 failed); slice2 Open draft <id> button timeout, no Draft editor (2 failed); slice3 in-app Close with dirty edits shows no confirm dialog, dialogSeen false (1 failed, 3 already-green); slice4 POST discard 404 vs 200 (5 failed) + Discard draft button timeout (1 failed).
Green evidence: backend pytest tests -> 46 passed (test_drafts 11 + patients 4 + physicians 5 + identity 17 + contracts 8 + health 1); ruff check + format clean (test_drafts E501/docstring wrapped, no behavior change); mypy new files clean (4 pre-existing S04 accounts.py RowMapping errors untouched); alembic downgrade -1 + upgrade head ok (0004->0003->0005 on dev DB); web tsc + vite build pass (dist js 209.18 kB/gzip 64.21 kB); playwright chromium full 26 passed (identity 7 + themes 3 + accessibility 8 + registration 3 + autosave 2 + draft-safety 2 + discard 1); firefox S07 specs 5 passed. Runtimes: Python 3.11.16, PostgreSQL 14.24 (psql client), Node 22.23.2, Playwright 1.63.0, pytest 9.1.1, chromium+firefox.
Review/limitations/unrun checks: make check mypy still red on pre-existing S04 accounts.py:244,291,344,400 RowMapping errors (untouched, left for S04 completion); ruff/tsc/vite green. .agents/agents/dev-backend.md + dev-frontend.md modifications are pre-existing environment artifacts, untouched. Discard takes If-Match only, no Idempotency-Key (plan lists idempotency for create/run/sign/note/addendum/recovery, not discard). Audit discard assertion uses interim direct audit_events read (S03 precedent) until S52 admin audit HTTP. Switching-drafts guard uses window.__xinsight_dirty flag (ponytail ceiling: lift into router state when wizard gains real routes). compose not runtime-tested. No full-firefox run (S07 specs only on firefox; full suite chromium-only).
Content approvals: n/a (synthetic draft notes/IDs only, never released).
Remaining work and next eligible session: S08 (assessment definitions/evaluator) unblocked; S09–S13 build on the S07 autosave path (getEncounter/patchEncounter + If-Match revision ownership — reuse it, no separate persistence); S14 follow-up entry; S15 eligible in parallel; S51 integrates discard with draft-set revision + jobs cancellation.

Session: S08 (status: awaiting_review)
Outcome and FR/NFR covered: Executable assessment definition contract + three draft review packages (FR-11–13, NFR-05) via approved T2/T1. Pure evaluate(definition, answers) distinguishes unanswered/partial/complete/not-assessed with scores None until complete (missing never zero); strict load_definition/evaluate validation (undeclared ids, out-of-range/wrong-type/bool values, mixed skip, unknown operators, recursive forbidden-expression keys); allowlisted ops {sum, max} only — diagnosis criterion logic and C-SSRS branching/alerts explicitly deferred to S09–S11 typed implementations. Draft diagnosis/PANSS/C-SSRS packages with recomputed source hashes, exact item prompts, uncertainties, independent examples; C-SSRS paraphrase status preserved. Authenticated GET /content/assessments/{type} serves released-only definitions (private, no-store); all three real packages are draft → 404; drafts retained on disk for review. Tests written/run by dev-test subagent, backend by dev-backend subagent, one slice per red–green cycle.
Files/migrations/content versions changed: backend/src/x_insight/assessments/__init__.py (new: evaluate/load_definition, ~160 lines); backend/src/x_insight/assessments/content.py (new: CONTENT_DIR/ALLOWED_TYPES/load_released + GET route, 86 lines); backend/src/x_insight/app.py (+2: mount content router); backend/tests/assessments/test_definitions.py (new: 13 tests, synthetic fixture only); backend/tests/http/test_content.py (new: 4 tests); content/assessments/{diagnosis,panss,cssrs}.json (new drafts: diagnosis-draft-1 3159B, panss-draft-1 6341B, cssrs-draft-1 5147B). No migrations, no frontend changes.
Approved seams exercised: T2 (pure evaluate/load_definition, no I/O) / T1 (content routes vs real PostgreSQL auth).
Red evidence: slice1 ModuleNotFoundError x_insight.assessments; slice2 4 passed/9 failed (DID NOT RAISE ValueError ×4, TypeError on str answer, ImportError load_definition ×3, max silently dropped); slice4 anon GET 404≠401 + ImportError content module (2 failed/2 passed). Each red observed before its minimum implementation.
Green evidence: make test-backend TEST=tests/assessments/test_definitions.py → 13 passed; TEST=tests/http/test_content.py → 4 passed; full make test-backend → 63 passed, 0 failed (19.34s). ruff check + format + mypy strict clean on all new/changed files. Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1.
Review/limitations/unrun checks: make check still red on pre-existing S04 identity/accounts.py RowMapping mypy errors (untouched, left for S04 completion). item_errors field present but always {} (violations raise ValueError instead — accepted v1 contract). Rule items referencing unanswered optional items are skipped while status stays complete (reviewed instruments must list scoring items as required). No browser/e2e in this session (no UI changed). compose not runtime-tested.
Content approvals: pending owner review — diagnosis-draft-1, panss-draft-1, cssrs-draft-1 (reviewer null, review_date null). Source hashes recomputed: schizophrenia-criteria.md 6d47f73b…1cecbd0/4463B, PANSS.md 644a291a…73ba83d2/6349B, CSSRS.md 610ecf93…bdc9ced/7035B. Nothing activated; synthetic fixtures test-only, never released.
Remaining work and next eligible session: S09 (diagnosis, needs diagnosis package approval), S10 (PANSS, needs PANSS approval), S11 (C-SSRS, needs C-SSRS approval) — generic infrastructure complete, clinical form work blocked per-package until review; S12–S15 eligible in parallel.

Session: S09 (status: complete)
Outcome and FR/NFR covered: Diagnosis threshold, warning ack, and reason-free bypass (FR-11, FR-16) via approved T2/T1/T9. Typed evaluate_diagnosis encodes all six A-F from schizophrenia-criteria.md; symptom count alone cannot satisfy; partial has no completed threshold; complete below-threshold requires attributed revision-tied ack (edits invalidate); bypass without reason stamps actor/time/status and survives resume; distinct Complete vs Bypass diagnosis UI reuses S07 autosave (If-Match, ~1s debounce, Saving…/Saved rev N). Tests written/run by dev-test subagent, backend by dev-backend subagent, frontend by dev-frontend subagent (2 overload retries; partial UI landed before report, finished with minimal glue), one slice per red–green cycle.
Files/migrations/content versions changed: backend/src/x_insight/assessments/diagnosis.py (new, typed evaluator); backend/src/x_insight/cases/encounters.py (PATCH stamps/validates warning_ack + bypass, 422 on forged, bypass clears ack); content/assessments/diagnosis.json (diagnosis-draft-1 → diagnosis-v1-released, released); backend/tests/assessments/test_diagnosis.py (new, 4 tests); backend/tests/http/test_diagnosis.py (new, 5 tests); backend/tests/http/test_content.py (diagnosis 200, panss/cssrs 404); web/src/app/App.tsx (DiagnosisSection + autosave wiring); e2e/diagnosis.spec.ts (new, 4 journeys). No migrations.
Approved seams exercised: T2 (pure diagnosis evaluator) / T1 (HTTP ack/bypass enforcement vs real PostgreSQL + released content) / T9 (browser threshold/bypass/resume vs real app).
Red evidence: slice1 ModuleNotFoundError x_insight.assessments.diagnosis; slice2 test_draft_definitions_not_exposed 200≠404 (legitimate release); slice3 3 failed (verbatim draft_data, forged 200≠422, missing actor_id); slice4 backend 3 passed/2 failed (bypass KeyError actor_id, forged 200≠422) + e2e region Diagnosis timeout (section not rendered); T9-2 follow-up strict-mode 3-element below-threshold match after wiring.
Green evidence: backend tests/assessments/test_diagnosis.py 4 passed; tests/http/test_diagnosis.py 5 passed; tests/http/test_drafts.py + test_content.py 15 passed; ruff check clean (after format 3 files), mypy clean on new/changed (4 pre-existing S04 accounts.py RowMapping errors untouched); web tsc + vite build pass (219.22 kB/gzip 66.63 kB); playwright e2e/diagnosis chromium 4 passed + firefox 4 passed; full chromium 30 passed. Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1, fastapi 0.141.1, sqlalchemy 2.0.54, Node 22.23.2, Playwright 1.63.0 chromium+firefox.
Review/limitations/unrun checks: make check still red on pre-existing S04 accounts.py:244,291,344,400 RowMapping mypy errors (untouched). Duplicate test_diagnosis basename requires split pytest runs (import-mismatch when combined). No fabricated diagnostic probability (threshold only, scores None). compose not runtime-tested. No full-firefox suite (diagnosis firefox only + chromium full).
Content approvals: diagnosis-v1-released sha256 632f35a77deafa0a6d2133b69612d0a079937bdef30a8b6451ec98e4d2aad64b, reviewer owner, review_date 2026-09-21, source schizophrenia-criteria.md 6d47f73b…1cecbd0/4463B; panss/cssrs remain draft pending (reviewer null).
Remaining work and next eligible session: S10 (needs PANSS approval), S11 (needs C-SSRS approval), S12 history/effects drafting, S13 notes (unblocked, S07 only), S14 follow-up (needs S09–S13), S15 DDI parse (eligible in parallel).

Session: S10 (status: complete)
Outcome and FR/NFR covered: PANSS without implicit minimum answers (FR-12, FR-20) via approved T2/T1/T9. Fresh 30 unanswered null total, explicit selection only, Skip persists not_assessed; all-1 7/7/16/30 and all-7 49/49/112/210 approved sums; one missing suppresses total, server rejects out-of-range/noninteger/bool/string, resume preserves completeness; subscales/items + previous-7-days window + prior-none historical display, no bands/change treatment gates. Tests written/run by dev-test subagent, backend by dev-backend subagent, frontend by dev-frontend subagent, one slice per red-green cycle.
Files/migrations/content versions changed: backend/src/x_insight/assessments/panss.py (new, PANSS_DEFINITION panss-v1 + evaluate_panss delegating to generic evaluate); backend/src/x_insight/cases/encounters.py (PATCH _apply_panss_validation, 422 INVALID_CONTENT on ValueError, skip shapes answers/not_assessed both accepted); content/assessments/panss.json (panss-draft-1 → panss-v1-released, released, definition 30 int 1-7 + 4 sum rules only); backend/tests/assessments/test_panss.py (new, 8 tests); backend/tests/http/test_panss.py (new, 3 tests); backend/tests/http/test_content.py (panss now 200, cssrs still 404); web/src/app/App.tsx (PanssSection + DraftEditor autosave reuse); e2e/panss.spec.ts (new, 3 journeys). No migrations.
Approved seams exercised: T2 (pure panss evaluator) / T1 (HTTP validation + released content vs real PostgreSQL) / T9 (browser skip/partial/resume vs real app).
Red evidence: slice1 ModuleNotFoundError x_insight.assessments.panss; slice2 already-green (sums via generic, 5 passed no code change); slice3 test_invalid_panss_rejected_server_side 200≠422 (verbatim store); slice4 e2e getByTestId panss-section 10s timeout element not found (6 failed chromium+firefox); test_draft_definitions_not_exposed panss 404≠200 legitimate release.
Green evidence: make test-backend TEST=tests/assessments/test_panss.py → 8 passed; TEST=tests/http/test_panss.py → 3 passed; TEST=tests/http/test_content.py → 4 passed; regression test_diagnosis 5 passed + test_drafts 11 passed; web npm run check clean + vite build 227.37kB; make test-e2e TEST=panss → 6 passed chromium+firefox; full make test-e2e → 66 passed. Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1, Node 22.23.2, Playwright 1.63.0 chromium+firefox.
Review/limitations/unrun checks: make check still red on pre-existing S04 accounts.py RowMapping mypy errors (untouched). Duplicate test_diagnosis/test_panss basenames require split runs. No default 1s, no hidden zeros, no illness-band gates, no change-formula gate. compose not runtime-tested.
Content approvals: panss-v1-released sha256 1ce753228ec6113a2c96e41cf5e1eec64775200a43620b4294fbc3b59f4311db, reviewer owner, review_date 2026-09-21, source docs/medical-docs/PANSS.md 644a291a…73ba83d2/6349B; cssrs remains draft pending.
Remaining work and next eligible session: S12 history/effects, S13 notes (unblocked), S14 follow-up (needs S09–S13), S15 DDI parse eligible in parallel.

Session: S11 (status: complete)
Outcome and FR/NFR covered: C-SSRS form and distinct results (FR-13, FR-20) via approved T2/T1/T9. Unanswered/skipped produce no assessed result (severity None); complete explicit negatives produce reviewed no-ideation severity 0; level-3 endorsed gives severity 3 without autofilling L1/L2; intensity/behavior/lethality recorded separately with no composite/risk score; current vs historical periods retained verbatim with missing-item guidance (never guessed negative); only approved flags (no_ideation/clinical_review/high_risk per CSSRS.md Interpretation, current L4/L5 or recent behavior = urgent) plus static emergency-protocol text (not calculated). Browser renders persistent role=alert review/urgent messages with text + keyboard focus; reload/autosave preserves responses and never turns skip into zero. Tests written/run by dev-test subagent, backend by dev-backend subagent, frontend by dev-frontend subagent, one red-green cycle per slice.
Files/migrations/content versions changed: backend/src/x_insight/assessments/cssrs.py (new, evaluate_cssrs L1-L5 strict bool + current/historical, intensity 1-5/0-5, behavior present+recent/historical, lethality actual 0-5/potential 0-2, severity max endorsed, flags only, no composite); backend/src/x_insight/cases/encounters.py (_apply_cssrs_validation, 422 INVALID_CONTENT, skip + answers shapes verbatim); content/assessments/cssrs.json (cssrs-draft-1 → cssrs-v1-released, released, definition cssrs-v1 L1-L5); backend/tests/assessments/test_cssrs.py (new, 9 tests); backend/tests/http/test_cssrs.py (new, 3 tests); backend/tests/http/test_content.py (cssrs now 200); web/src/app/App.tsx (CssrsSection + DraftEditor autosave reuse, preserves intensity/behavior/lethality); e2e/cssrs.spec.ts (new, 4 journeys). No migrations.
Approved seams exercised: T2 (pure cssrs evaluator) / T1 (HTTP value/period validation + released content vs real PostgreSQL) / T9 (browser skip/negatives/level-3/high-risk resume vs real app).
Red evidence: slice1 ModuleNotFoundError x_insight.assessments.cssrs + HTTP 200≠422; slice2 2 failed (severity None≠3, undeclared intensity); slice3 3 failed (KeyError details/flags); slice4 e2e cssrs-section 10s timeout element not found (8 failed chromium+firefox).
Green evidence: make test-backend TEST=tests/assessments/test_cssrs.py → 9 passed; TEST=tests/http/test_cssrs.py → 3 passed; TEST=tests/http/test_content.py → 4 passed; regression test_panss 8 passed + test_drafts 11 passed; web npm run check + vite build pass (Node 22.23.2); make test-e2e TEST=cssrs → 8 passed chromium+firefox; TEST=panss → 6 passed. Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1, Playwright 1.63.0 chromium+firefox.
Review/limitations/unrun checks: make check still red on pre-existing S04 accounts.py:244,291,344,400 RowMapping mypy errors (untouched). Duplicate test_cssrs basename requires split runs. Intensity/behavior/lethality UI is preserve-verbatim only (no dedicated inputs); paraphrase wording retained, not authorized form. compose not runtime-tested. No full-suite e2e (cssrs + panss only).
Content approvals: cssrs-v1-released sha256 041137eeb5e50cd94f2a6287192cfc73a93b6383f2553ff276bc3062c001b2e7, reviewer owner, review_date 2026-09-21, source docs/medical-docs/CSSRS.md 610ecf937795c695828d15376a2555e3e153f97307f7b758dc9c1e7b6bdc9ced/7035B; definition cssrs-v1.
Remaining work and next eligible session: S12, S13, S14 (now unblocked on S09–S11), S15 eligible in parallel.

Session: S12-frontend (status: complete)
Outcome and FR/NFR covered: Structured history + adverse-effects UI and e2e content wiring (FR-14, FR-20–21) via approved T1/T9, reusing the S07 autosave path (getEncounter/patchEncounter + If-Match, ~1s debounce, role=status Saving…/Saved rev N, read-only for non-author, Reload draft). HistorySection (data-testid history-section, region labelled History) renders analysis-visible structured history distinctly from page notes; generic over GET /content/history (field ids + severity enums from the released definition, never hardcoded); 404 (draft unreleased) shows honest pending-review/unavailable state and sends no history/effects block. EffectsSection (data-testid effects-section, region labelled Adverse effects) covers all four FR-21 effects with status selects (data-testid effects-status-<id>) + severity selects (data-testid effects-severity-<id>, enabled only when present, empty otherwise); status change explicitly clears obsolete severity client-side (absent/not_assessed send severity null); present-without-severity is withheld from the wire like the S11 C-SSRS period rule, never 422ing a draft. No BARS/SAS/AIMS questionnaires. Minimal optional phone text input (null clears, no country validation) wired to PATCH /patients/{id} with revision/If-Match + Saving…/Saved (rev N)/Stale/Save-failed, physician-only in UI. Reconciliation rendered as explicit object state (pending/confirmed select stored verbatim, never auto-confirmed; no S14 copy semantics). E2e-only fix: playwright.config.ts backend webServer env sets X_INSIGHT_HISTORY_CONTENT_DIR=../tests/fixtures/content/history (synthetic released fixture); production default unchanged, draft awaiting_review never activated. Tests by dev-test/dev-backend reused unchanged (one slice per red–green cycle).
Files/migrations/content versions changed: web/src/app/api.ts (+getHistoryContent, +patchPatientPhone); web/src/app/App.tsx (+EFFECT_IDS/labels, history/effects/recon types + fromStored/serialize/dirtyKey helpers, HistorySection, EffectsSection, DraftEditor state/refs/hydration/handlers/saveNow payload/saved-refs/Saved-condition/discard/phone block, role prop plumbed from PatientsSection); playwright.config.ts (+e2e-only history content dir env + comment). No backend, migration, or content-release changes (backend/tests/http/test_history.py still 8 passed untouched).
Approved seams exercised: T1 (released-only GET /content/history + PATCH validation vs real PostgreSQL) / T9 (browser vs real app + real PostgreSQL).
Red evidence: make test-e2e TEST=history → 4 failed, getByTestId('history-section') not found (both journeys × chromium+firefox). After sections/effects implementation: journey 1 failed at reload (effects-status-tardive_dyskinesia "" not "absent"); probe showed GET /content/history 200 in-browser but HistorySection stuck on "Loading history content…" — root cause: encounter-load .then reset definition state after the faster definition fetch resolved (fixed by making the fetch effect sole owner of definition state; reset moved to effect start on patient change).
Green evidence: web npm run check (tsc) clean; vite build 245.22 kB/gzip 73.78 kB; make test-e2e TEST=history → 4 passed chromium+firefox; TEST=registration → 6 passed; full make test-e2e → 78 passed (32.2s); make test-backend TEST=tests/http/test_history.py → 8 passed. Disposable probes (deleted): absent×4 PATCH persisted with server provenance + definition_version synthetic-history-v1; recon pending stored verbatim {"status":"pending"} and preserved across reload; phone saved rev 2, cleared to null rev 3, reload empty. Runtimes: Node 22.23.2, Playwright 1.63.0 chromium+firefox, PostgreSQL 14.24 (xinsight_test), Python 3.10.12 shell (backend pins 3.11.16 per S01).
Review/limitations/unrun checks: History field inputs render only with a released definition (default dev shows honest unavailable; exact clinical field ids await owner release). Withheld effects/history blocks stay dirty until flushed (Saved still advertises acknowledged note/diag writes only). Phone/recon verified via disposable probe only (no committed e2e asserts them; history.spec asserts sections + absent round-trip + read-only). Pre-existing S04 mypy RowMapping errors and unrelated working-tree changes (backend/graphify from sibling agents) untouched. No full make check (backend scope untouched). Two 401 console lines in probe are the pre-login /me probes, pre-existing.
Content approvals: n/a — history-effects-v0.1-draft remains awaiting_review (reviewer null); e2e uses tests/fixtures/content/history/history.json synthetic-history-v1 (synthetic_fixture true, never shipped as default).
Remaining work and next eligible session: S13 (notes), S14 (follow-up entry: baseline copy + reconciliation semantics + phone/history/effect pages), S20 (medications/DDI UI); S14 must define valid reconciliation values and copy semantics — current UI stores any pending/confirmed object verbatim with no copy logic.

Session: S12 (status: awaiting_review)
Outcome and FR/NFR covered: Structured history + four adverse effects mechanics (FR-14, FR-20–21) via approved T2/T1/T9. Draft history-effects-v0.1-draft stays awaiting_review; all runtime behavior verified against conspicuously synthetic fixture only (tests/fixtures/content/history/history.json, synthetic-history-v1). History values persist with server provenance (actor/recorded_at/encounter_revision), known/false distinct from unknown/not-assessed (null); undeclared + FR-14-excluded regimen fields (dose/unit/route/frequency/active/stopped) -> 422 with revision/draft untouched. All four effects require present/absent/not_assessed; present requires released severity, absent/not_assessed requires null; status change requires explicit severity clear (omitted severity key -> 422). Optional phone text update via PATCH /patients/{id} (physician-only, If-Match, null clears, no country validation) + history_reconciliation object shape guard (bare strings -> 422; enum/copy semantics left to S14). Released-only GET /content/history (anon 401, draft -> 404). Browser HistorySection/EffectsSection reuse S07 autosave (debounce, Saved rev N, read-only for non-author, reload preserves); analysis-visible history never relabeled from page notes; no BARS/SAS/AIMS questionnaires mandated. Tests run by dev-test, backend by dev-backend, frontend by dev-frontend, one red-green cycle per slice.
Files/migrations/content versions changed: content/history/history-effects-v0.1-draft.json (unchanged, awaiting_review); backend/src/x_insight/cases/history.py (regimen guard + reconciliation guard); backend/src/x_insight/cases/encounters.py (medications/reconciliation guards in PATCH chain); backend/src/x_insight/cases/patients.py (PATCH /patients/{id} phone-only); backend/src/x_insight/cases/history_content.py (new, GET /content/history); backend/src/x_insight/app.py (mount history-content router); backend/tests/http/test_history.py (3 -> 8 tests: undeclared, regimen-excluded, phone, reconciliation-shape, content-route); e2e/history.spec.ts (new, 2 journeys); web/src/app/api.ts (getHistoryContent, patchPatientPhone); web/src/app/App.tsx (HistorySection/EffectsSection/reconciliation/phone via S07 autosave); playwright.config.ts (e2e-only X_INSIGHT_HISTORY_CONTENT_DIR=../tests/fixtures/content/history). No migrations, no released defaults. Plus review-only ruff-format whitespace in test_history.py.
Approved seams exercised: T2 (synthetic typed validation, no I/O) / T1 (HTTP PATCH/content/phone vs real PostgreSQL) / T9 (browser saved-state vs real app + real PostgreSQL).
Red evidence: make test-backend TEST=tests/http/test_history.py baseline 3 passed; after dev-test additions 4 passed/4 failed (regimen 200!=422; phone 404!=200; reconciliation 200!=422; content-route 404!=401); e2e/history.spec.ts 2 failed on getByTestId('history-section') timeout (element not found).
Green evidence: make test-backend TEST=tests/http/test_history.py -> 8 passed; TEST=tests/http/test_drafts.py -> 11 passed; make test-e2e TEST=history -> 4 passed (chromium+firefox); web npm run check clean; vite build 245.22 kB/gzip 73.78 kB (per dev-frontend); full make test-e2e -> 78 passed (per dev-frontend). make check: ruff + format clean; mypy clean on changed files, still red only on pre-existing S04 identity/accounts.py:244,291,344,400 RowMapping errors (untouched). Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1, Node 22.23.2, Playwright 1.63.0 chromium+firefox.
Review/limitations/unrun checks: History content NOT approved (awaiting_review, reviewer null); production default content dir has no released history.json so history PATCH 422s / content 404s outside synthetic env — e2e-only env override in playwright.config.ts, never shipped as default. Phone/reconciliation verified via disposable probes + backend tests (no committed phone/recon e2e asserts). S14 must define reconciliation values/copy semantics. compose not runtime-tested. No full backend suite (history+drafts only). Unrelated working-tree changes (graphify-out churn, sibling backend/docs edits) untouched; no commit.
Content approvals: pending owner review — history-effects-v0.1-draft (reviewer null, review_date null). Synthetic fixture synthetic-history-v1 only, never released.
Remaining work and next eligible session: S13 (notes, unblocked), S14 (follow-up: baseline copy + reconciliation semantics + phone/history/effect pages; needs S09–S13), S15 DDI parse eligible in parallel. S12 mechanics done; release blocked on owner approval of history-effects package.

Session: S13 (status: complete)
Outcome and FR/NFR covered: Attributed page notes with proven separation (FR-16, FR-22) via approved T1/T9. Author POST/GET /encounters/{id}/notes persists page/author/time/text visibly after resume; same Idempotency-Key retry returns the original note (one row, audit encounter.note.add scoped by actor+encounter+key, 409 on key reuse with different body); non-author/admin POST 403, anon 401, client author/time ignored (server-derived), no edit routes (PUT/PATCH/DELETE 404/405), blank page/text 422; notes live in encounter_notes (never draft_data) with ANALYSIS_EXCLUDED_TOP_LEVEL_KEYS={'notes'} + project_encounter_for_analysis() exclusion gate for future snapshots; browser NotesSection (demographics default) renders page/author/time with React-escaped text, separate from history-section.
Files/migrations/content versions changed: backend/migrations/versions/0006_notes.py (new, encounter_notes + index, head 0006; EXPECTED_SCHEMA_REVISION 0005->0006); backend/src/x_insight/cases/notes.py (new, POST/GET notes + idempotent audit + exclusion constant); backend/src/x_insight/app.py (mount notes router); backend/src/x_insight/db.py (expected revision 0006); backend/tests/http/test_notes.py (new, 5 T1 tests); e2e/notes.spec.ts (new, 3 T9 journeys); web/src/app/api.ts (PageNote/listNotes/addNote); web/src/app/App.tsx (NotesSection + mount before HistorySection). Plus FK-safe TRUNCATE fixups in test_identity/content/physicians/drafts/patients/diagnosis/panss/cssrs/history fixtures (encounter_notes first) and one TestClient.delete(json=) fix in test_notes.py.
Approved seams exercised: T1 (HTTP vs real PostgreSQL) / T9 (browser vs real app + real PostgreSQL).
Red evidence: make test-backend TEST=tests/http/test_notes.py baseline 5 failed (POST /notes 404 route missing; later 422-vs-404 shape); e2e notes 3 journeys timeout on missing notes-section; draft-safety/discard chromium regressions from duplicate role=status (strict-mode 2 elements) until notes loading left role=status.
Green evidence: make test-backend TEST=tests/http/test_notes.py -> 5 passed; notes+drafts+identity -> 33 passed; notes+drafts+patients+history+content+physicians -> 37 passed; cssrs/diagnosis/panss http -> 11 passed; assessments -> 34 passed; make test-e2e TEST=notes -> 6 passed (chromium+firefox); notes+history chromium -> 5 passed; discard+draft-safety+autosave chromium -> 5 passed; web npm run check clean; vite build 247.47 kB/gzip 74.25 kB. Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1, Node 22.23.2, Playwright 1.63.0.
Review/limitations/unrun checks: make check ruff+format clean; mypy clean on changed files, still red only on pre-existing S04 identity/accounts.py:244,291,344,400 RowMapping errors (untouched). Full make test-backend unblocked file-by-file but whole-suite collection still trips pre-existing duplicate basenames (tests/http vs tests/assessments test_cssrs/test_diagnosis/test_panss import mismatch) — pre-existing, unrelated to S13. Full make test-e2e not run (time-boxed; chromium subsets above green). compose not runtime-tested. No clinical content added.
Content approvals: n/a (no content).
Remaining work and next eligible session: S14 (follow-up: baseline copy + reconciliation semantics + phone/history/effect pages; needs S09-S13 done), S15 DDI parse eligible in parallel. Record S40/S41/S59 as mandatory end-to-end note-noninterference checks (notes never in projections/prompts/CPTs/DDI/proposals).

Session: S14 (status: complete)
Outcome and FR/NFR covered: Shared chart + follow-up draft entry (FR-20–23) via approved T1/T9. Physician POST /patients/{id}/encounters {baseline_encounter_id} + Idempotency-Key creates an own follow_up draft (201, revision 1, idempotent replay/409 on changed body) only against a signed baseline (draft baseline 409, missing/cross-patient baseline 404, archived patient 409, admin 403, anon 401, missing key 422); any active physician/admin shares reads via GET single + GET list (chronology ORDER BY created_at,id) while PATCH/discard stay author-only (403); baseline history values + medications copy verbatim with server-restamped provenance (creator/revision 1) and history_reconciliation opens {status pending, baseline_encounter_id}; PANSS/C-SSRS never copy (follow-up starts unanswered, baseline keeps scores, prior scores render as historical with dates, PANSS selects stay empty); two physicians hold separate drafts on one baseline with cross-edit denied; read-side baseline_changed flags a newer signed record on GET single + list without rebasing linkage or blocking edits (sign enforcement deferred to S49/S51); chart UI shows per-encounter kind/state badges, Start follow-up (physician-only, signed baselines), per-encounter open buttons, changed-baseline reconcile notice, follow-up baseline-copy note, prior-scores history, and exact 'Proposal generation unavailable.' until reasoning exists (no fake proposal). Tests written/run by dev-test, backend by dev-backend, frontend by dev-frontend, one red–green cycle per slice.
Files/migrations/content versions changed: backend/migrations/versions/0007_followup.py (new, encounters.baseline_encounter_id UUID NULL no FK, head 0007; EXPECTED_SCHEMA_REVISION 0006->0007 in db.py); backend/src/x_insight/cases/encounters.py (FollowupCreate, POST create_followup with session/physician/CSRF/Idempotency-Key/advisory-lock/fingerprint-replay guards, baseline copy with restamped history provenance + verbatim medications + pending recon, _payload baseline_encounter_id + baseline_changed via EXISTS self-join on (created_at,id) ordering wired into get/list/create/patch/discard); backend/tests/http/test_followup.py (new, 9 T1 tests slices 1-3); e2e/followup.spec.ts (new, 2 T9 journeys, test-only signed seeding via inline psql UPDATE/INSERT, no sign route added); web/src/app/api.ts (Encounter += baseline_encounter_id/baseline_changed + createFollowup); web/src/app/App.tsx (PatientChart chart-section/chart-encounters/chart-encounter-<id>/chart-badge-<id>/chart-baseline-changed/start-followup-<id>/open-encounter-<id>, PatientsSection selectedEncounterId wiring, DraftEditor initialEncounterId + FollowupExtras followup-baseline-note/prior-scores/generation-status). No clinical content.
Approved seams exercised: T1 (HTTP vs real PostgreSQL) / T9 (browser vs real app + real PostgreSQL).
Red evidence: slice1 make test-backend TEST=tests/http/test_followup.py -> 4 failed, assert 405 == 201/409 at POST (only GET list registered); slice2 -> 4 passed + 3 failed KeyError 'history'/'history_reconciliation' (empty draft, setup PATCH 200/POST 201 pass); slice3b -> 8 passed + 1 failed KeyError 'baseline_changed' on GET single (slice3a coexistence already green, no code change); slice4 make test-e2e TEST=followup -> 4 failed (2 journeys x chromium+firefox) getByTestId('chart-section') not found timeout 5000ms.
Green evidence: make test-backend TEST=tests/http/test_followup.py -> 9 passed; test_drafts.py 11 passed + test_history.py 8 passed + test_notes.py 5 passed + test_patients.py 4 passed + test_content.py 4 passed (sequential runs); make test-e2e TEST=followup -> 4 passed; TEST=history -> 4 passed; TEST=notes -> 6 passed; web npm run check (tsc) clean; vite build 251.79 kB/gzip 75.30 kB; alembic upgrade head 0006->0007 + downgrade -1 + upgrade head ok; ruff check + format clean on all S14 files; mypy clean on encounters.py/db.py. Runtimes: Python 3.11.16, PostgreSQL 14.24 (psql 14.24), Node 22.23.2, Playwright 1.63.0 chromium+firefox.
Review/limitations/unrun checks: make check still red on pre-existing S04 identity/accounts.py:244,291,344,400 RowMapping mypy errors (untouched) + sibling DDI ingestion E501 (untouched, out of scope). Temporary signed fixtures stay test-only (backend SQL UPDATE, e2e inline psql); no bypass-sign production route added. Sign/baseline-ack enforcement deferred to S49/S51 (indicator is read-only; PATCH still 200). Parallel backend suites in one turn deadlock on TRUNCATE (run sequentially; observed once, reruns green). Whole-suite collection still trips pre-existing duplicate test basenames (http vs assessments); full make test-backend / full make test-e2e not run (file/journey subsets above green). compose not runtime-tested. Unrelated working-tree items untouched and uncommitted here: backend/src/x_insight/ddi + backend/tests/ddi (sibling S15 work), .agents/agents/document.md deletion + commit.md untracked (pre-existing environment artifacts), graphify-out churn.
Content approvals: n/a (synthetic history fixture synthetic-history-v1 test-only via X_INSIGHT_HISTORY_CONTENT_DIR; no clinical content released).
Remaining work and next eligible session: S15 DDI parse eligible in parallel; S20 medications/DDI UI builds on the copied medications list; S40/S41/S59 mandatory end-to-end note-noninterference checks; S49/S51 sign + baseline-reconciliation enforcement (indicator already surfaces stale baselines).

Session: S15 (status: complete)
Outcome and FR/NFR covered: Real Sitagliptin monograph parsed through the T3 ingestion interface (FR-14 offline ingestion mechanics, NFR-05 versioned provenance). `build(source_dir, terminology=None, review_manifest=None)` discovers .txt sources in stable relative order, locates the actual interaction section ignoring Medscape navigation/summary headings (Interactions/Interaction Checker/bare severity labels), preprocesses repeated page chrome, BOM, and line wrapping while preserving original line spans, parses entries per severity category, and validates parsed counts against the monograph's declared counts (0/4/92/70). A removed entry fails count validation (declared 4/parsed 3) with a count_mismatch anomaly report and release-ineligible result — never truncation/padding. CLI `python -m x_insight.ddi build` artifacts deep-equal the library result. No SQL, no runtime checker, no LLM/network. Tests written/run by dev-test role, implementation by dev-backend role, one slice per cycle (role files loaded into task subagents — see limitations).
Files/migrations/content versions changed: backend/src/x_insight/ddi/ingestion.py (new: build, parser_version ddi-ingest-1); backend/src/x_insight/ddi/__main__.py (new: argparse build subcommand, candidates.json/report.json, exit 0/1/2); backend/src/x_insight/ddi/__init__.py (new: build re-export); backend/tests/ddi/test_ingestion.py (new, 5 T3 tests); backend/tests/ddi/fixtures/sitagliptin-provenance.json (new: verified provenance — original sha256 e7c9bc45ed5b3f829dfe8e29b015ee727645db2f3ed6cad8de99fea2dcd4022f/44041B/1265 content lines/UTF-8 BOM, headings 97/99/122/658 = 0/4/92/70, nav headings 74-89 ignored, Adverse Effects@1005/Warnings@1076 terminators, page-chrome patterns, representative spans erdafitinib 101-105, captopril 205-215 (chrome 209/213 inside, paragraphs 206-207 + 215), oxandrolone 916-925, clonidine 708-714, ofloxacin 525-529 AND 912-914, vanadium 1001-1003); backend/tests/ddi/fixtures/synthetic-terminology-input.json (new, conspicuously synthetic placeholder bytes, hashed only, never parsed/released). No migrations.
Approved seams exercised: T3 (library build(...) and CLI over real source files and reviewed/provenance inputs).
Red evidence: S15-A `make test-backend TEST=tests/ddi/test_ingestion.py` -> collection error `ModuleNotFoundError: No module named 'x_insight.ddi'`; S15-B same command -> `.F`, AssertionError "entries are not yet parsed: no 'captopril' entry"; S15-D same command -> `....F`, "No module named x_insight.ddi.__main__; 'x_insight.ddi' is a package and cannot be directly executed". S15-C1/C2 red phases legitimately overtaken (entry parsing necessarily landed in S15-B's green; count/anomaly plumbing in S15-A's green) — documented deviation, tests kept at full strength; C2 carries a mutation proof (truncate/pad/swallow mutants of build all fail the pinned literals).
Green evidence: named check `make test-backend TEST=tests/ddi/test_ingestion.py` -> 5 passed per slice and after the final lint fix (5 passed in 0.22s); `uv run ruff check src tests` clean (after fixing 12 S15 E501s); `uv run ruff format --check src tests` 46 formatted; `uv run mypy src/x_insight/ddi` Success (strict); full backend suite with the 3 pre-existing colliding files ignored -> 111 passed (real PostgreSQL); CLI smoke `cd backend && PYTHONPATH=src uv run python -m x_insight.ddi build ...`: clean byte-identical copy exit 0, both JSON artifacts, count_checks 0/4/92/70 all ok, 166 entries; erdafitinib-removed copy (provenance-recorded, original lines 101-105+blank 106 dropped) exit 1 with report.json written, count_mismatch declared 4/parsed 3, anomaly span {99,99}; web npm run check + build pass. Runtimes: Python 3.11.16, PostgreSQL 14.24, pytest 9.1.1.
Review/limitations/unrun checks: whole-suite `make test-backend` collection still aborts on pre-existing duplicate test basenames (tests/http vs tests/assessments test_cssrs/test_diagnosis/test_panss import mismatch; reproduced at pristine b509108 and 75e698a — not S15); `make check` mypy src still red on pre-existing S04 identity/accounts.py:244,291,344,400 RowMapping errors (untouched). plan.md §6.2's literal CLI line lacks PYTHONPATH=src and the package has no build-system/entry point, so the documented invocation needs `PYTHONPATH=src` today (flagged for owner/S16; plan.md untouched). Missing --terminology path raises FileNotFoundError (exit 1) instead of an argparse usage error (exit 2) — unspecified edge, recorded for S16/S18 polish. Entry grammar carries one recorded smallest-honest rule: a paragraph-start line continuing a prior line's comma/semicolon-unfinished sentence is not an entry header; validated to reproduce exactly 166 headers on the fixture. `missing_interaction_section` anomaly contract-defined, exercised in S16. Terminology/review_manifest recorded as path+sha256 provenance only (resolution S17, publish S18). dev-test/dev-backend/commit agent types are not registered in the harness — role files .agents/agents/*.md were loaded into task subagents instead. compose not runtime-tested.
Content approvals: n/a — source parsing expectations only (fixture provenance records original checksum/counts/spans; original source unchanged); no clinical content created or released; DDI release review gate remains S18.
Remaining work and next eligible session: S16 (extend parser across real source formats; exercise missing_interaction_section; complete 128-file discovery with enumerated anomalies; fix plan.md CLI invocation note with owner), then S17 (controlled concept/alias resolution via terminology input), S18 (reviewed publish + DDI migrations); S20 medications/DDI UI per its dependencies.

Session: S21 (status: complete)
Outcome and FR/NFR covered: Safe XMLBIF draft import/inspection (FR-31, FR-37, NFR-05) via approved T5. validate_xmlbif(bytes)->dict reuses versioned BNs/schema.xml, preserves source bytes/hash, returns ordered nodes/edges (GIVEN-only) plus separate XSD report; inspection activatable False always (XSD success never executable). Hostile inputs (DOCTYPE/entity, remote SYSTEM, oversize, malformed, non-BIF root) raise bounded XmlInputError with line/column; BN-04/BN-08 inspectable as drafts with missing_definitions + proposed_parents_ignored (34 entries in BN-04, never edges); networks list preserves state/parent/PROPERTY order, export_source identity returns exact bytes; multi-network and decision/utility kinds reported nonactivatable, never silently selected/dropped. Tests written/run by dev-test, implementation by dev-backend, one red-green cycle per slice (agent files loaded into task subagents).
Files/migrations/content versions changed: backend/src/x_insight/models/__init__.py (new); backend/src/x_insight/models/validation.py (new, 278 lines: validate_xmlbif, export_source, XmlInputError, MAX_XML_BYTES=1048576); backend/tests/models/test_xml_validation.py (new, 11 T5 tests); backend/pyproject.toml (+lxml==6.1.3); backend/uv.lock (+lxml 6.1.3). No migrations, no BNs/ changes, no clinical content.
Approved seams exercised: T5 (model validation; real pinned lxml + versioned schema, synthetic + real draft bytes as inputs).
Red evidence: slice1 pytest tests/models/test_xml_validation.py -> ModuleNotFoundError x_insight.models; slice2 same file -> 4 failed/2 passed (DOCTYPE raised XMLSchemaValidateError not XmlInputError, remote DID NOT RAISE, malformed raw XMLSyntaxError, <FOO/> DID NOT RAISE); slice3 -> 6 passed/2 failed (missing proposed_parents_ignored/missing_definitions reporting); slice4 -> 8 passed/3 failed (missing networks/export_source/kind tracking).
Green evidence: make test-backend TEST=tests/models/test_xml_validation.py -> 11 passed (0.26s); BNs/test_schema.py -> PASS 79 checks; ruff check + format --check on src/x_insight/models + tests/models clean; mypy src/x_insight/models strict clean. Runtimes: Python 3.11.16 (venv), system 3.10.12, lxml 6.1.3, pytest 9.1.1, ruff 0.16.8, mypy 2.3.1.
Review/limitations/unrun checks: make check full still red on pre-existing S04 identity/accounts.py RowMapping mypy errors (untouched); whole-suite make test-backend still trips pre-existing duplicate basenames (http vs assessments, out of scope); flat nodes/edges derive from FIRST network only (documented v1 flattening, authoritative data is networks list); no HTTP routes/graph UI (S24); no semantic/admission checks (S22); compose not runtime-tested; dev-test/dev-backend/commit types loaded via task subagents (not registered harness names).
Content approvals: n/a (synthetic fixtures only; BN-04/BN-08 read read-only as draft inputs, unaltered, never activated).
Remaining work and next eligible session: S22 (semantics/admission: cycles, dimensions, normalization, decision/utility drafts stay inactive, limits/diagnostics); S25 before S24 per plan; S26-38 drafting after S25.

Session: S22 (status: complete)
Outcome and FR/NFR covered: Model semantics + admission + activation gating (FR-31–32, FR-37, NFR-04) via approved T5. check_semantics(validated) runs after XSD: distinct codes cycle/missing_definition/wrong_dimensions/unnormalized/empty_outcomes (+non_finite/out_of_range/xsd_invalid), tolerance 1e-6, deterministic sorted errors; valid root/parent ordering passes, decision/utility emit unsupported_kind and stay drafts; all 11 supplied BNs/BN-04..14 remain nonexecutable. decide_activation(validated, package, semantic_report, limits, admission_report) separates import (draft inspectable) from activation (executable + admitted + complete reviewed package): missing_mappings/prompt/template, note_source_path (any allowed_source_paths containing notes), unreviewed_package (review decision approved + reviewer + date required; reviewed=true alone insufficient), undeclared_query/state, unsafe_expression (conservative __/import/eval/exec/;/notes hook for S25), semantic_not_executable, missing_package, admission_rejected. check_admission + DEFAULT_LIMITS engineering bounds from 2026-09-22 corpus measurements (max 60681B/32 nodes/880 cells, BN-04 largest; limits 262144B/64 nodes/10000 cells ~4x/2x/11x headroom, not clinical) with deterministic sorted diagnostics; BN-04 admitted under defaults. Tests written/run by dev-test, implementation by dev-backend, frontend checked by dev-frontend (no UI needed, S24 owns admin UI), one red-green cycle per slice (role files loaded into task subagents).
Files/migrations/content versions changed: backend/src/x_insight/models/semantics.py (new: check_semantics, is_safe_expression, decide_activation, check_admission, DEFAULT_LIMITS, S25 hooks documented); backend/tests/models/test_model_admission.py (new, 17 T5 tests). No migrations, no BNs/ changes, no clinical content.
Approved seams exercised: T5 (model validation/inference/replay; real pinned lxml + versioned schema, synthetic + real draft bytes).
Red evidence: slice1 ModuleNotFoundError x_insight.models.semantics; slice2 7 passed/1 failed test_decision_utility_remain_drafts assert True is False (no unsupported_kind); slice3 ImportError decide_activation (6 failed/8 passed); slice4 ImportError DEFAULT_LIMITS/check_admission + TypeError limits kwarg (3 failed/14 passed). Each red observed via make test-backend TEST=tests/models/test_model_admission.py before its minimum implementation.
Green evidence: make test-backend TEST=tests/models/test_model_admission.py -> 17 passed; TEST=tests/models/test_xml_validation.py -> 11 passed; combined 28 passed; BNs/test_schema.py -> PASS 79 checks; ruff check + format clean on models; mypy src/x_insight/models strict clean. Runtimes: Python 3.11.16, lxml 6.1.3, pytest 9.1.1, PostgreSQL 14.24 (unchanged).
Review/limitations/unrun checks: make check full still red only on pre-existing S04 identity/accounts.py RowMapping mypy errors (untouched); whole-suite make test-backend still trips pre-existing duplicate basenames (out of scope); FIRST-network-only v1 flattening documented; no HTTP routes/graph UI (S24); no CPT inference yet (S23); compose not runtime-tested; content/questions/hospitalization/* untracked sibling work untouched, not committed here. dev-test/dev-backend/dev-frontend/commit types loaded via task subagents (not registered harness names).
Content approvals: n/a (synthetic fixtures only; BNs read read-only, never activated; no patient mappings invented).
Remaining work and next eligible session: S23 (CPT validation + exact inference, needs S22 green); S25 before S24 per plan (contract validation hooks to fill: validate_patient_mappings, validate_cpt_contract, validate_prompt_template, validate_queries — S22 provides presence/shape only); S26-38 drafting after S25.

Session: S26 (status: awaiting_review)
Outcome and FR/NFR covered: Hospitalization question draft package R1 (FR-30–35) as complete review dossier, not an active placeholder. Specific question, 7-day-horizon proposal, 3-state review-prompt outputs (routine/urgent/emergency, never admission orders or involuntary eligibility), 5-node fixed discrete graph over SuicidalConcern/AggressionConcern/SupportSupervision/PriorCourse/HospitalizationReview, estimation-only prompt, synthetic reference tables with explicit no-validity provenance, predefined template branches plus deterministic urgent flag that never waits for LLM, ordinary/urgent/emergency/required-missing/permitted-unknown cases and independently enumerated numerical fixture. Verified by dev-test-role task subagent (role files loaded into task subagent; dev-test/dev-backend/commit types not registered).
Files/migrations/content versions changed: content/questions/hospitalization/{network.xml (new, 5288B, sha256 40a611f5d7c0ac1a20c7036882d3c74080b723c809dd7f656f1b869a5ff62323),manifest.json,prompt.txt,template.json,examples.json,review.json} (new, all awaiting_review). No backend/migration/BN changes; no frontend changes (no UI in this content session, dev-frontend not applicable).
Approved seams exercised: T5 (XSD via versioned BNs/schema.xml + semantics via models/semantics.py on draft bytes; numerical fixture by independent plain enumeration outside implementation under test).
Red evidence: n/a (content-drafting session; no behavior tests per AGENTS.md — no Markdown/constant-mirroring tests written).
Green evidence: PYTHONPATH=backend/src validate_xmlbif(network.xml) -> XSD valid True, nodes in manifest order, 4 GIVEN-only edges, missing_definitions [], semantics executable True errors []; manifest network_hash/size match actual bytes; independent stdlib-only recomputation: single-row lookup row 54 [60.0,30.0,10.0], transpose rows 0 [85.0,12.0,3.0] vs 67 [5.0,35.0,60.0] differ, all four clinical reference_row_lookups match cells, prior-marginal [0.566,0.2929,0.1411] max diff 3.3e-16 <= 1e-9; manifest covers all plan 7.2 fields, prompt forbids plan/applicability/full-record work, template has no LLM disposition text. Runtimes: Python 3.11.16 (venv), lxml 6.1.3.
Review/limitations/unrun checks: S25 package validator does not exist yet (no content/questions schema; S25 pending) so dossier validation is T5 XSD+semantics + independent enumeration only; S23 inference-harness replay pending; sibling S22 extended semantics.py (check_admission/decide_activation) concurrently — untouched, verified present. make check / backend suite / e2e not run (no code changed; content-only). compose not runtime-tested.
Content approvals: pending owner review — hospitalization-v0.1-draft (reviewer null, review_date null, clinical_activation_allowed false). Reference tables synthetic structural placeholders, never patient estimates; no empirical probabilities manufactured; no guideline rating converted. Owner questions Q1–Q8 in review.json (horizon, vocabulary, missingness, 3 proposed fields, PriorCourse definition, wording/no-argmax, R1/R3 boundary, v0.2 additions).
Remaining work and next eligible session: Owner review of Q1–Q8; S39 workflow admission after review. S27–S38 remain independently eligible (each needs its own dossier); S25 validator can use this package as first conformance example.

Session: S27 (status: awaiting_review)
Outcome and FR/NFR covered: Pharmacotherapy question draft package R2 (FR-14, FR-30–35) as complete review dossier, not an active placeholder and not a complete initial-choice network. Documents BN-04 established-review scope vs R2 initial-selection scope with per-node verdicts and all 34 metadata-only proposed_parent entries; fixed 5-node graph (PriorTreatmentContext/VulnerabilityContext/PreferenceBarrier/ExposureModifierFlag -> 22-state InitialMedicationConsideration in Table 6 oral order) with explicit GIVEN edges; provisional catalog binding ingredient-<lowercase-name> pending catalog release; FR-14-excluded dose/route/etc. resolved by redesign (ExposureModifierFlag presence-only, no hidden fields); estimation-only prompt with DDILookup prohibition; template renders full consideration distribution with no argmax selection plus deterministic caution pointers and DDI pointer; 3 context cases + needs_clarification case with proposed (unapproved) expectations and independent hand-arithmetic fixtures (row lookups + standalone 2-node/2-parent engine checks). No backend/frontend/test code written (content-drafting session per AGENTS.md; dev-test/dev-frontend/dev-backend not applicable — no behavior changed).
Files/migrations/content versions changed: content/questions/pharmacotherapy/{network.xml (new, 19142B, sha256 77dd8dd050720c7d517eb52a68ab2cf94ae48ccb54ea760d184560389fa0951d),manifest.json,prompt.txt (5512B),template.json,examples.json,review.json} (new, all awaiting_review). No backend/migration/BNs/ changes (BN-04 untouched, verified via git status); sibling content/questions/hospitalization/* untouched.
Approved seams exercised: T5 (XSD via versioned BNs/schema.xml + semantics via models/semantics.py on draft bytes; numerical fixture by hand arithmetic outside implementation under test).
Red evidence: n/a (content-drafting session; no behavior tests per AGENTS.md — no Markdown/constant-mirroring tests written). One generator defect found and fixed during drafting: unescaped <lowercase> in network PROPERTY broke XSD parsing (XmlInputError line 77); reworded to ingredient-lowercase-name, re-validated green.
Green evidence: validate_xmlbif(network.xml) -> XSD valid True, node order == manifest declared_node_order, states match, 4 GIVEN edges; check_semantics -> executable True, errors []; manifest network_hash matches actual bytes; child TABLE 108 rows x 22 cols, worst row err 2.2e-16; examples rows verified against file bytes — row 0 Chlorpromazine 6.0/Asenapine 4.0, row 63 Quetiapine 6.0/Thiothixene 4.0, row 31 Trifluoperazine 6.0/Risperidone 4.0, rest 4.5; standalone P(B=yes)=0.22, 7/11, asymmetric P(C)=0.278 rechecked; decide_activation (review stub) -> activatable false with unreviewed_package (correct); check_admission -> admitted true (19142B/5 nodes/2389 cells within 262144B/64/10000). Runtimes: Python 3.11.16 (venv; system 3.10.12 for file ops), lxml 6.1.3.
Review/limitations/unrun checks: S25 package validator does not exist yet so dossier validation is T5 XSD+semantics + file-consistency + hand arithmetic only; S23 inference-harness replay pending; decide_activation presence/shape gaps in evidence reflect the minimal stub passed (manifest itself carries mappings/prompt/template/queries for S25). make check / backend suite / e2e not run (no code changed; content-only). compose not runtime-tested.
Content approvals: pending owner review — pharmacotherapy-v0.1-draft (reviewer null, review_date null, clinical_activation_allowed false). Reference tables synthetic structural placeholders, never patient estimates; no empirical probabilities manufactured; no guideline rating converted. Owner questions Q1–Q8 in review.json (admissible set + catalog binding, vulnerability anchors, preference instrument, exposure anchors, clozapine inclusion, wording/no-argmax, R2/DDI boundary, horizon).
Remaining work and next eligible session: Owner review of Q1–Q8 (esp. Q1 catalog set and Q5 clozapine); S39 workflow admission after review. S28–S38 remain independently eligible; S25 validator can use R1+R2 packages as conformance examples.

Session: S29 (status: awaiting_review)
Outcome and FR/NFR covered: High-suicide clozapine question draft package R4 (FR-13, FR-30–35) as complete review dossier, not an active placeholder. Retains BN-08's 14 nodes/states/12 GIVEN edges verbatim (on-point drafting lead, no metadata-only parents); resolves 10 missing priors with synthetic root rows and 4 deterministic 0/1 workflow tables with soft argmax-preserving placeholders (verified generator reproduces all 44 source rows exactly; originals untouched). Gate triad (confirmed schizophrenia AND clinician substantial-risk judgment AND documented persistence) with urgent/exposure required and 5 parallel-review inputs optional; false gate yields not_applicable (never a negative posterior), unknown gate yields needs_clarification stopping later questions. Estimation-only prompt (14 tables, 54 rows, 213 cells); template renders 6 pathway branches plus needs_clarification/not_applicable with deterministic urgent flag independent of LLM latency and no suicide probability output. Ten synthetic gate/rendering cases plus hand-worked fixture (row lookup, P(Indication=Present)=0.101375 marginal, transpose/asymmetry detectors). Content-drafting session per AGENTS.md: no behavior tests; validation run by dev-test-role task subagent; no backend/frontend/migration changes (dev-backend/dev-frontend not applicable).
Files/migrations/content versions changed: content/questions/high_suicide_clozapine/{network.xml (new, 11356B, sha256 53923f50991636ea1ebd799cc2821576635040476d02441428f3999663047203),manifest.json,prompt.txt (8075B),template.json,examples.json,review.json} (new, all awaiting_review). No backend/migration/BNs/ changes (BN-08 untouched, verified via git status).
Approved seams exercised: T5 (XSD via versioned BNs/schema.xml + semantics/admission via models/semantics.py on draft bytes; numerical fixture by independent stdlib enumeration outside implementation under test).
Red evidence: n/a (content-drafting session; no behavior tests per AGENTS.md — no Markdown/constant-mirroring tests written).
Green evidence: validate_xmlbif -> XSD valid True, 14 nodes == declared order, states match, 12 GIVEN edges tuple-exact; check_semantics -> executable True, errors []; check_admission -> admitted True (11356B/14 nodes/213 cells within 262144B/64/10000); manifest network_hash == sha256(network.xml); DEFINITION row counts [1]x10+[8,4,8,24] == cpt_contract; 0/21 source paths contain notes; likelihood disabled; all 54 rows sum to 1.0 within 1e-9; all 5 clinical reference_row_lookups match file cells exactly; prior-marginal 0.101375 exact (diff 0.0); prompt covers 14 nodes/schema; template covers 6 states + needs_clarification + not_applicable with urgent flag; no disposition/dosage instruction text. Runtimes: Python 3.11.16 (venv), lxml 6.1.3.
Review/limitations/unrun checks: S25 package validator does not exist yet so dossier validation is T5 XSD+semantics + file-consistency + independent arithmetic only; S23 inference-harness replay pending. make check / backend suite / e2e not run (no code changed; content-only). compose not runtime-tested. Untracked sibling content/questions/lai_indication_choice/network.xml (not this session) left untouched.
Content approvals: pending owner review — high_suicide_clozapine-v0.1-draft (reviewer null, review_date null, clinical_activation_allowed false). Reference tables synthetic structural placeholders, never patient estimates; no empirical probabilities manufactured; no guideline rating or C-SSRS level converted into a probability. Owner questions Q1–Q9 in review.json (C-SSRS windows, gate composition, persistence meaning, 8 proposed fields, required/optional classification, both-usage rationale, no-horizon/no-probability rule, wording/no-argmax, R4/R7/F5 boundaries).
Remaining work and next eligible session: Owner review of Q1–Q9; S39 workflow admission after review. S28, S30–S38 remain independently eligible; S25 validator can use R1+R2+R4 packages as conformance examples.

Session: S32 (status: awaiting_review)
Outcome and FR/NFR covered: Established-case clozapine question draft package R7 (FR-10, FR-30–35) as complete review dossier, not an active placeholder. Registration-scoped 18-node subset of BN-07 retained verbatim for states/edges (12 roots + 6 derived review flags, 17 GIVEN edges); five ongoing-treatment nodes (ExposureInterpretationConcern, ClozapineTrialAdequate, ResidualSymptomsOnClozapine, ClozapineExposureReview, PersistentSymptomsReview) excluded with rationale to keep R7 distinct from F5 no_improvement_clozapine (own key/gate/network/prompt/template). Gate is registration AND established clinical status (explicit FR-10 applicability input, context-only) AND confirmed schizophrenia AND persistent symptoms AND two adequate suboptimal trials with different drugs; first-time status yields not_applicable with no posterior (never a fake negative). Trial adequacy is clinician-recorded four-state categories with agents/dates/response/adherence evidence only; TRRIP 6-week/therapeutic-dosage/80-percent points are background reference, never stored regimen numbers (FR-14 redesign). Twelve-week duration, severity/function, willingness, monitoring plan, and medical review are optional with disclosure and included only when defined and collected. Estimation-only prompt (18 tables, 92 child rows, 309 cells); template renders six pathway branches plus needs_clarification/not_applicable with deterministic urgent flag independent of LLM latency and no resistance-probability output. Eight synthetic gate/rendering cases (first-time false gate, four established-applicable branches, urgent, missing trial constituent, permitted-unknown optional set) plus hand-worked fixture (single-row lookup, P(Additional=Present)=0.2338, P(TwoAdequate=Yes)=0.10229, transpose/asymmetry detectors). Tests written/run by dev-test subagent, backend validated by dev-backend subagent, frontend checked by dev-frontend subagent per session instruction; no behavior tests for wording/constants per AGENTS.md.
Files/migrations/content versions changed: content/questions/established_case_clozapine/{network.xml (new, 15234B, sha256 8f5c8ec20f0e0997d2a7ecefd7b44896f69504eabbdb981e55eedd17203304dd),manifest.json,prompt.txt (10356B),template.json,examples.json,review.json} (new, all awaiting_review). No backend/migration/BNs/ changes (BN-07 untouched, verified via git status); sibling content/questions/{high_suicide_clozapine,hospitalization,pharmacotherapy}/* untouched; untracked sibling dirs aggression_clozapine + lai_indication_choice left untouched.
Approved seams exercised: T5 (XSD via versioned BNs/schema.xml + semantics/admission via models/semantics.py on draft bytes; numerical fixture by independent stdlib enumeration outside implementation under test).
Red evidence: n/a (content-drafting session; no behavior tests per AGENTS.md — no Markdown/constant-mirroring tests written). One generator defect found and fixed during drafting: parents-dict unpacking KeyError 'Yes' in the table generator (states/parents swapped); fixed, re-validated green.
Green evidence: validate_xmlbif -> XSD valid True, 18 nodes == manifest declared_node_order, 17 GIVEN edges tuple-exact, missing_definitions [], unsupported_kinds []; check_semantics -> executable True, errors []; check_admission -> admitted True (15234B/18 nodes/309 cells within 262144B/64/10000); manifest network_hash == sha256(network.xml); DEFINITION row counts [1]x12+[32,8,4,16,8,24] == cpt_contract; independent stdlib recomputation: TwoAdequate row 0 [0.88,0.12], Additional marginal 0.2338 diff 2.8e-17, TwoAdequate marginal 0.10229 diff 1.4e-17, pathway transpose rows match file cells; dev-test: tests/models/test_xml_validation.py 11 passed + test_model_admission.py 17 passed; dev-frontend: npm run check pass, no frontend changes. Runtimes: Python 3.11.16 (venv; system 3.10.12 for stdlib probes), lxml 6.1.3, Node per web check.
Review/limitations/unrun checks: S25 package validator does not exist yet so dossier validation is T5 XSD+semantics + file-consistency + independent arithmetic only; S23 inference-harness replay pending. make check full / full backend suite / full e2e not run (no code changed; content-only; file/journey subsets above green). compose not runtime-tested.
Content approvals: pending owner review — established_case_clozapine-v0.1-draft (reviewer null, review_date null, clinical_activation_allowed false). Reference tables synthetic structural placeholders, never patient estimates; no empirical probabilities manufactured; no guideline rating/scale total converted into a probability. Owner questions Q1–Q8 in review.json (adequacy operationalization + FR-14, gate + established status, windows, 11 proposed fields, required/optional + willingness/monitoring scoping, both-usage rationale, R7/F5 boundary + exclusions, wording/no-argmax + urgent flag).
Remaining work and next eligible session: Owner review of Q1–Q8; S39 workflow admission after review. S28, S30–S31, S33–S38 remain independently eligible; S25 validator can use R1+R2+R4+R7 packages as conformance examples.
